# CLAUDE.md

<!-- Add your custom instructions below. Repowise will never modify anything outside the REPOWISE markers. -->
<!-- Examples: coding style rules, test commands, workflow preferences, constraints -->

## Wohin KI-Arbeitsdokumente gehoeren

**Briefs, Journals, Plaene, Inventare, Rollout- und Statusberichte gehoeren NICHT in dieses
Repo.** Sie leben im privaten Journal-Repo nebenan:

```
../MeisterStack-Journal/
  claude/   <- Arbeitsstand: CLI-Guide, Feature-Listen, refactor-inventory/-log, Berichte
  design/   <- Briefs: README.md (die Warteschlange), Meilenstein-, Nightshift-, Chaos-, HA-, Storage-Briefs
```

Regeln fuer kuenftige Agenten:

- Neues Dokument dieser Art anlegen oder fortschreiben -> in `../MeisterStack-Journal/design/`
  bzw. `.../claude/`, und dort committen. Das Journal-Repo ist eigenstaendig, hat kein Remote
  und wird nicht gepusht.
- `docs/claude/` und `docs/design/` im Hauptrepo sind aufgeloest und in der `.gitignore`
  gesperrt. Nicht neu anlegen — auch nicht "nur kurz".
- Was im Hauptrepo bleibt, gehoert Silas: `docs/ACKNOLEDGEMENT.md`, `docs/AI_GUIDELINES.md`,
  `docs/ARCHITECTURE.md`, `docs/MOTIVATION.md`, `docs/TECHSTACK.md`, `docs/diagrams/`.
  Diese Dateien sind Projektdokumentation, keine Agentenausgabe — nicht verschieben.
- `origin` ist das Uni-Repo `UniStuttgart-IKR/MeisterStack`. Nicht dorthin pushen, ohne dass
  Silas es ausdruecklich sagt.

<!-- REPOWISE:START — Do not edit below this line. Auto-generated by Repowise. -->
## Codebase Intelligence for MeisterStack (Repowise)

Indexed by [Repowise](https://repowise.dev). Last indexed: 2026-08-28 (commit 184918a). Confidence: 100%.
### How to work in this repo

- **Trust the index.** `verified: true` means the bytes were checked against the live tree, so never re-read those lines. Re-read only on `bounds: "approximate"`, `_meta.stale_warning`, `search_method: "bm25"` or `confidence: "low"`; `index_behind: true` alone is informational.
- **Pre-edit, not instead-of-edit.** These tools decide *which* files to read and edit. Claude Code requires a raw Read of any file you will Edit, and that Read is correct and expected.
- **Noisy commands** (tests, builds, `git log`/`diff`, searches, listings): prefer `repowise distill <cmd>` — same command, exit code preserved, errors-first output. A `[repowise#<ref>: N lines omitted]` marker is recoverable via `repowise expand <ref>` (add `-q <regex>` to filter); never re-run the command to see omitted output.
- **Recording a decision** you had to reason out: `repowise decision add --title T --decision D` records it without prompting and prints the id (`--format json` to parse it back). It lands `proposed`, for a person to confirm.

### Tools

| Tool | When and why |
|------|--------------|
| `get_answer(question)` | First call for any how/where/why question. Cite `confidence: "high"` or `grounding: "extracted"` directly; `degraded` means judge by `retrieval_quality`. `symbol_bodies` has live bodies. |
| `get_context(targets=[...])` | Triage card for files/modules/symbols: docs, signatures, hotspot, fix history. No source bytes — `include=["skeleton"]` for the whole file verified, `["callers"|"decisions"]` for depth. Batch targets. |
| `get_symbol(id)` | **Follow-up, not an entry point** — one verified body for an id a prior response named (`path.py::Name`, `path.py:140-180`, `repowise#<hex>`). Never walk a file symbol by symbol; Read it. |
| `search_codebase(query)` | Hybrid search, auto-routed by query shape; force with `mode=symbol|path|concept|hybrid`. A hit whose `sources` are `[fts]` only has no semantic agreement, so verify it. |
| `get_why(query, targets?)` | Why the code is shaped this way: decision records, git archaeology, rationale comments. Call before a refactor or a pattern divergence. |
| `get_risk(targets, changed_files?)` | What history says about touching these files. PR mode (`changed_files`) leads with a `directive`: read `will_break` / `missing_cochanges` / `missing_tests` / `tests_to_run` first. |
| `get_change_risk(revspec, extensions?, exclude_patterns?)` | Defect score for a whole commit or `base..head` range, from its diff on the live checkout. Lead with `risk_percentile`. Scores a range; `get_risk` scores paths. |
| `get_health(targets?, include?)` | Defect / maintainability / performance scores and findings. Self-check the files you touched before finishing. |
| `get_dead_code()` | Confidence-tiered unreachable files / unused exports / zombie packages. For cleanup sweeps, not targeted fixes. |
| `get_overview()` | Architecture map. Call once, first, in an unfamiliar repo; skip it after that. |

### Architecture
**Files:** 69 | **Lines:** 5181 | **Monorepo:** 16 packages
MeisterStack is a multi-package rust codebase of 69 files, split across 16 packages. Execution starts at components/agent/src/main.rs, components/cli/src/main.rs, components/cloud-controller/src/main.rs and 14 other entry points. Start here when reading the codebase. Ranked by PageRank over the import graph: the files most of the codebase ultimately depends on.

### Key modules
- `shared/agent-api/src` — shared/agent-api/src · shared/agent-api/src/types · shared/common/src · shared/controller-api/src · shared/macros/src
**Language:** rust |…
- `components/agent/src` — components/agent/src · components/cli/src · components/cloud-controller/src
**Language:** rust | **Files:** 14 | **Public symbols:** 195 /…
- `plugins/nfs/src` — plugins/nfs/src
**Language:** rust | **Files:** 1 | **Public symbols:** 1 / 3
Covers the 1 source files in plugins/nfs/src
- `drivers/cgroup/src` — drivers/cgroup/src · drivers/cloud-hypervisor/src · drivers/crosvm-gpu/src · drivers/filesystem/src · drivers/linux-network/src ·…
- `shared/proto` — shared/proto · shared/proto/src
**Language:** rust | **Files:** 2 | **Public symbols:** 0 / 1
Covers the 2 source files in 2 directories…
- `components/cluster-controller/src` — components/cluster-controller/src
**Language:** rust | **Files:** 1 | **Public symbols:** 0 / 1
Covers the 1 source files in…

### Entry points
- `components/agent/src/main.rs`
- `components/cloud-controller/src/main.rs`
- `components/cluster-controller/src/main.rs`
- `components/cli/src/main.rs`
- `shared/macros/src/lib.rs`
- `drivers/lvm-thin/src/lib.rs`

### Code health
Three co-equal signals: defect risk 6.01/10 avg, hotspot health 6.01/10 (stable), worst `components/agent/src/provision.rs` at 2.81/10 · maintainability 8.29/10 · performance risk 25 open static I/O-in-loop / N+1 findings. Detail: `get_health()`.

Critical files:
- `drivers/nvrm/src/lib.rs` — brain method (create) — impact −0.3
- `drivers/cloud-hypervisor/src/lib.rs` — brain method (build_vm_config) — impact −0.3
- `components/cluster-controller/src/main.rs` — brain method (main) — impact −0.3
- `components/cli/src/client.rs` — brain method (request) — impact −0.3

<!-- REPOWISE:END -->
