[package]
name = "meister-filesystem-driver"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
agent-api = { package = "meister-agent-api", path = "../../shared/agent-api" }
async-trait = { workspace = true }
anyhow = { workspace = true }
nix = { workspace = true }
# For `qemu-img info --output=json`, and only that. Hand-reading it was a
# mistake once: the document has a NESTED "virtual-size" for the file node
# before the real one, so the first match is the wrong number.
serde_json = { workspace = true }
tokio = { workspace = true }
tracing = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
uuid = { workspace = true }
